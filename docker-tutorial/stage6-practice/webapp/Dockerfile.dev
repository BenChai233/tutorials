# 开发环境 Dockerfile
FROM maven:3.9-eclipse-temurin-17

# 设置工作目录
WORKDIR /app

# 复制 pom.xml 和下载依赖
COPY pom.xml .
RUN mvn dependency:go-offline -B

# 暴露端口
EXPOSE 8080

# 启动命令（使用 Spring Boot DevTools 支持热重载）
CMD ["mvn", "spring-boot:run"]

