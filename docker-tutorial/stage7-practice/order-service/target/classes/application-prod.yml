spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

# 生产环境日志配置
logging:
  level:
    root: WARN
    com.docker.practice.orderservice: INFO
  file:
    name: /app/logs/order-service.log
    max-size: 10MB
    max-history: 30

