# Docker 生产实践项目 - 环境变量配置
# 复制此文件为 .env 并根据需要修改

# 应用配置
SPRING_PROFILE=prod
TZ=Asia/Shanghai

# 端口配置
GATEWAY_PORT=8080
USER_SERVICE_PORT=8081
ORDER_SERVICE_PORT=8082
MYSQL_PORT=3306

# MySQL 配置
MYSQL_ROOT_PASSWORD=123456
MYSQL_DATABASE=testdb
MYSQL_USER=appuser
MYSQL_PASSWORD=apppass

# 服务配置
USER_SERVICE_REPLICAS=2
ORDER_SERVICE_REPLICAS=2
GATEWAY_SERVICE_REPLICAS=1

# 资源限制
USER_SERVICE_CPU_LIMIT=1.0
USER_SERVICE_MEMORY_LIMIT=512M
ORDER_SERVICE_CPU_LIMIT=1.0
ORDER_SERVICE_MEMORY_LIMIT=512M
GATEWAY_SERVICE_CPU_LIMIT=0.5
GATEWAY_SERVICE_MEMORY_LIMIT=256M

# 镜像仓库配置（用于 CI/CD）
REGISTRY_URL=localhost:5000
IMAGE_TAG=latest

# Jenkins 配置（可选）
JENKINS_URL=http://localhost:8080

# GitLab CI 配置（可选）
CI_REGISTRY=registry.gitlab.com
CI_REGISTRY_USER=your-username
CI_REGISTRY_PASSWORD=your-password

